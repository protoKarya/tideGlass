# SPDX-License-Identifier: AGPL-3.0-or-later
#
# Scope Graph — birth certificate for the tideGlass Targeted GuideStone.
# Declares which ecosystem components contribute to this artifact.
#
# Pattern source: lithoSpore/artifact/scope.toml (LTEE instance)
# Version 0.1.0 — as-built UniBin composition (Phase 4).

[guidestone]
name = "tideglass-guidestone"
version = "0.1.0"
target = "GPS Platform — Bin Chen Lab, MSU (Cell 2026)"
created = "2026-06-06"
standard = "TARGETED_GUIDESTONE_STANDARD v1.0"
targets_file = "data/targets/gps_validation_targets.toml"
graph_file = "graphs/tideglass_guidestone.toml"

# --- Contributing Springs ---

[[spring]]
name = "healthSpring"
contributes = [
    "Hill dose-response (IC50 validation, Exp001/Exp093)",
    "MATRIX scoring (Fajgenbaum reversal framework, Exp090)",
    "Anderson geometry (tissue propagation prediction)",
    "Population PK Monte Carlo (compound distribution modeling)",
    "ChEMBL bioactivity pipeline (JAK panel, Exp093)",
    "ADDRC HTS pipeline (hit scoring, Exp091)",
    "Selectivity index (compound ranking, Exp092)",
    "RGES enrichment scoring (novel — extends score_panel)",
    "MCTS tree search (novel — chemical space optimization)",
]
modules = ["tideglass-rges", "tideglass-screen", "tideglass-molsearch", "tideglass-octad", "tideglass-nf"]
papers = ["Chen2017", "Zeng2021", "Xing2026"]

[[spring]]
name = "wetSpring"
contributes = [
    "ChEMBL data fetch (handle_chembl_fetch via NestGate)",
    "PubChem data fetch (handle_pubchem_fetch via NestGate)",
    "NCBI GEO fetch (L1000 expression profiles)",
    "NestGate content-addressing (BLAKE3 on ingested data)",
    "Provenance trio (session + step + completion on every fetch)",
    "GCTx/HDF5 streaming parse (novel — extends NestGate pattern)",
]
modules = ["tideglass-rcl", "tideglass-gps4drug", "tideglass-screen"]
papers = ["Subramanian2017"]

[[spring]]
name = "neuralSpring"
contributes = [
    "Deep learning model training patterns",
    "Multi-network architecture (RCL co-training)",
    "SMILES encoding to learned representation",
    "Model checkpoint verification (BLAKE3 chain)",
    "Training convergence monitoring",
]
modules = ["tideglass-rcl", "tideglass-gps4drug"]
papers = ["Xing2026"]

[[spring]]
name = "hotSpring"
contributes = [
    "Anderson disorder analogy (tissue geometry for drug propagation)",
    "Effective dimension mapping (d_eff for target tissues)",
    "Conformational energy landscapes (compound flexibility)",
]
modules = ["tideglass-nf"]
papers = ["Anderson analogy", "Xing2026 (tissue extension)"]

# --- Seven Science Modules ---
# Each module validates a specific claim from the paper chain.
# All modules dispatch through the tideglass UniBin binary.

[[module]]
name = "rges_correlation"
binary = "tideglass"
ipc_method = "science.rges_screen"
data_dir = "artifact/data/chen_2017_rges"  # awaiting GPS JSON conversion
expected = "validation/expected/module1_rges.json"  # awaiting Chen 2017 benchmark
tier1_notebook = ""  # Python parity deferred
paper = "Chen 2017 — Reversal of cancer gene expression (Nat Commun 8:16022)"
doi = "10.1038/ncomms16022"
claim = "RGES correlates with IC50 (r=0.52, P=2.3e-3 in MCF7)"
depends_on = []

[[module]]
name = "rcl_noise_cleaning"
binary = "tideglass"
ipc_method = "science.rcl_select"
data_dir = "artifact/data/lincs_vcap_t1"  # awaiting GPS JSON conversion
expected = "validation/expected/module2_rcl.json"  # awaiting fixture milestone
tier1_notebook = ""  # Python parity deferred
paper = "Xing 2026 — GPS (Cell, RCL component)"
doi = "10.1016/j.cell.2026.02.023"
claim = "RCL improves VCAP_t1 profile signal-to-noise vs raw LINCS"
depends_on = []

[[module]]
name = "expression_prediction"
binary = "tideglass"
ipc_method = "science.gps4drug_predict"
data_dir = "artifact/data/gps4drug_heldout"  # awaiting GPS JSON conversion
expected = "validation/expected/module3_prediction.json"  # awaiting fixture milestone
tier1_notebook = ""  # Python parity deferred
paper = "Xing 2026 — GPS (Cell, GPS4Drug component)"
doi = "10.1016/j.cell.2026.02.023"
claim = "GPS4Drug predicts expression from structure (R² on held-out test set)"
depends_on = ["rcl_noise_cleaning"]

[[module]]
name = "reversal_screening"
binary = "tideglass"
ipc_method = "science.compound_screen"
data_dir = "artifact/data/zinc_hcc_screen"  # awaiting CAS data load
expected = "validation/expected/module4_screening.json"  # awaiting fixture milestone
tier1_notebook = ""  # Python parity deferred
paper = "Xing 2026 — GPS (Cell, screening validation)"
doi = "10.1016/j.cell.2026.02.023"
claim = "ZINC screening recovers known HCC actives (enrichment AUC)"
depends_on = ["expression_prediction"]

[[module]]
name = "mcts_optimization"
binary = "tideglass"
ipc_method = "science.mcts_optimize"
data_dir = "artifact/data/molsearch_hcc"  # awaiting CAS data load
expected = "validation/expected/module5_mcts.json"  # awaiting fixture milestone
tier1_notebook = ""  # Python parity deferred
paper = "Xing 2026 — GPS (Cell, MolSearch component)"
doi = "10.1016/j.cell.2026.02.023"
claim = "MCTS optimizes HCC lead: IC50 4uM to 0.5uM, selectivity maintained"
depends_on = ["expression_prediction"]

[[module]]
name = "octad_parity"
binary = "tideglass"
ipc_method = "science.octad_benchmark"
data_dir = "artifact/data/octad_hcc_baseline"  # awaiting CAS data load
expected = "validation/expected/module6_octad.json"  # awaiting fixture milestone
tier1_notebook = ""  # Python parity deferred
paper = "Zeng 2021 — OCTAD (Nat Protocols 16:728-753)"
doi = "10.1038/s41596-020-00430-z"
claim = "GPS screening exceeds OCTAD repurposing for HCC targets"
depends_on = ["reversal_screening"]

[[module]]
name = "nf_extension"
binary = "tideglass"
ipc_method = "science.nf_score"
data_dir = "artifact/data/nf_disease_signatures"  # awaiting NF Data Portal ingest
expected = "validation/expected/module7_nf.json"  # awaiting fixture milestone
tier1_notebook = ""  # Python parity deferred
paper = "Novel (no prior publication — first NF application of GPS)"
doi = ""
claim = "GPS reversal scoring identifies candidates for NF1-driven tumors"
depends_on = ["reversal_screening"]

# --- Primals (Tier 3, optional graceful degradation) ---

[[primal]]
name = "rhizoCrypt"
tier = 3
required = false
purpose = "DAG provenance chain for each module execution"

[[primal]]
name = "loamSpine"
tier = 3
required = false
purpose = "Certificate chain for model weight verification"

[[primal]]
name = "sweetGrass"
tier = 3
required = false
purpose = "Attribution braid (6 papers, LINCS program, ChEMBL, NF Data Portal)"

[[primal]]
name = "toadStool"
tier = 3
required = false
purpose = "GPU streaming dispatch for RCL training + MCTS tree search"

[[primal]]
name = "nestGate"
tier = 2
required = false
purpose = "Content-addressed data fetch (LINCS L1000, ChEMBL, PubChem, ZINC)"

[[primal]]
name = "barraCuda"
tier = 2
required = false
purpose = "WGSL shader dispatch for RGES batch scoring and MCTS evaluation"

# --- Foundation Threads ---

[[foundation_thread]]
id = "03"
name = "Immunology / Drug Discovery"
datasets = ["lincs_l1000_level5", "chembl_jak_panel", "nf_data_portal", "zinc_screened_subset"]

[[foundation_thread]]
id = "07"
name = "Anderson Framework"
datasets = ["anderson_tissue_geometry"]
notes = "d_eff for nerve sheath (NF, d=2.4) and liver (HCC) tissue"

[[foundation_thread]]
id = "10"
name = "Provenance / Spore Ingestion"
datasets = []
notes = "Model weight provenance chain — novel category for ML artifacts"

# --- Source Metadata ---

[source]
repo = "git@git.primals.eco:protoKarya/tideGlass.git"
repo_https = "https://git.primals.eco/protoKarya/tideGlass.git"
branch = "main"
ecosystem_repo = "git@github.com:sporeGarden/ecoPrimals.git"
ecosystem_repo_https = "https://github.com/sporeGarden/ecoPrimals.git"
rust_toolchain = "stable"
rust_target = "x86_64-unknown-linux-musl"
containerfile = ""
license = "AGPL-3.0-or-later"
