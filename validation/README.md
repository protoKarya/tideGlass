# validation/ -- Python-first reproduction (Phase 0-2)

This directory contains Python notebooks and scripts that reproduce the
original GPS platform claims. Each module validates a specific paper claim.

The Python validation runs standalone (no primals needed). It exists to
prove that published results are reproducible from primary data before
we rebuild sovereign.

Once Rust modules pass parity with Python validation, this directory
becomes the Tier 1 reference baseline.
