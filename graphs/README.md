# graphs/ -- Deploy graph TOMLs for NUCLEUS composition

Deploy graphs define how tideGlass modules compose with primals.
Follow the biomeOS BYOB deploy graph standard from ecoPrimals.

The guideStone graph (tideglass_guidestone.toml) is the primary
composition for full validation runs.
